name: Build and Publish Arkeo Data Engine (Provider) Docker image

on:
  push:
    branches: ["main"]
    tags: ["v*"]
    paths:
      - "provider-core/**"
      - ".github/workflows/arkeo-data-engine-provider-docker.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "provider-core/**"
      - ".github/workflows/arkeo-data-engine-provider-docker.yml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write   # Required for pushing to GHCR

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: image-tags
        run: |
          tags="ghcr.io/${{ github.repository_owner }}/arkeo-data-engine-provider:latest,ghcr.io/${{ github.repository_owner }}/arkeo-data-engine-provider:${GITHUB_SHA}"
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            tags="${tags},ghcr.io/${{ github.repository_owner }}/arkeo-data-engine-provider:${GITHUB_REF_NAME}"
          fi
          echo "tags=${tags}" >> "$GITHUB_OUTPUT"

      - name: Build and push arkeo-data-engine-provider image
        uses: docker/build-push-action@v6
        with:
          context: ./provider-core
          file: ./provider-core/Dockerfile
          push: ${{ github.event_name == 'push' }}
          build-args: |
            APP_VERSION=${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || github.sha }}
          tags: ${{ steps.image-tags.outputs.tags }}
